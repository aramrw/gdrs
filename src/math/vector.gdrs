// Module: src/math/vector.gdrs

obj Vec2:
	x: i64
	y: i64

impl Vec2:
	fn new(x: i64, y: i64) -> Vec2:
		return Vec2{ x: x, y: y }

	fn dot(self, other: Vec2) -> i64:
		return (self.x * other.x) + (self.y * other.y)
