fn main():
	log!("--- 1. Range Iteration with .for_each ---")
	(0..5).for_each(|x| log!("Item:", x))

	log!("--- 2. Range Iteration with for loop ---")
	for x in (0..5):
		let mapped = x * 10
		log!("Mapped item (x * 10):", mapped)
